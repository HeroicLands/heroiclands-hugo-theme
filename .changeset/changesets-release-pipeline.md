---
---

Releasing runs from changesets rather than by hand (#30). Repo tooling only —
the published tarball is `layouts`, `static`, `data`, `theme.toml` and
`CHANGELOG.md`, none of which this changes — so it declares no bump.
